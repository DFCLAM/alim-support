# Issue #2 - ALIM 2 statistics

To adjust the conf. for your environment, edit the file `env.py` locally, or export the corresponding vars.
To avoid storing a cleartext password, use 
```
. read-passwords.sh
```

